const wrapper = document.querySelector('.window-wrapper');
function showPixWindow() {
  wrapper.classList.add('show-pix');
}
function showAboutMeWindow() {
  wrapper.classList.remove('show-pix');
}